name: meta

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "17 6 * * 1"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  validate:
    name: validate org metadata
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # ubuntu-slim ships a minimal toolset; PyYAML is not preinstalled.
      - name: Install PyYAML
        run: python3 -m pip install --break-system-packages --quiet pyyaml

      - name: Parse every YAML file
        run: |
          python3 - <<'PY'
          import pathlib, sys
          import yaml
          bad = 0
          for p in sorted(pathlib.Path(".").rglob("*.yml")) + sorted(pathlib.Path(".").rglob("*.yaml")):
              try:
                  yaml.safe_load(p.read_text())
              except Exception as exc:  # noqa: BLE001
                  print(f"{p}: {exc}")
                  bad += 1
              else:
                  print(f"ok  {p}")
          sys.exit(1 if bad else 0)
          PY

      - name: All external actions are SHA-pinned
        run: python3 tools/check_action_pins.py .

      - name: Relative Markdown links resolve
        run: python3 tools/check_links.py .

      - name: The organisation profile stays a landing page
        run: |
          set -e
          lines=$(wc -l < profile/README.md)
          echo "profile/README.md is $lines lines"
          if [ "$lines" -gt 60 ]; then
            echo "::error::the organisation profile is a landing page, not a specification."
            echo "Move detail into docs/ (PRODUCT.md, MATURITY.md, ARCHITECTURE_INVARIANTS.md)."
            exit 1
          fi

      - name: Required org files exist
        run: |
          set -e
          for f in profile/README.md CONTRIBUTING.md CODE_OF_CONDUCT.md SECURITY.md \
                   SUPPORT.md LICENSE-MIT LICENSE-APACHE NOTICE \
                   .github/PULL_REQUEST_TEMPLATE.md \
                   .github/ISSUE_TEMPLATE/config.yml \
                   docs/PRODUCT.md docs/MATURITY.md \
                   docs/ARCHITECTURE_INVARIANTS.md docs/CI_CONVENTIONS.md \
                   docs/RELEASE.md docs/DEPENDENCY_POLICY.md; do
            [ -f "$f" ] || { echo "missing: $f"; exit 1; }
            echo "ok  $f"
          done
